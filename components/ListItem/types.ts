export interface ListItemProps {
  isSelected?: boolean,
  leftContent?: string,
  leftIcon?: string,
  noPadding?: boolean,
  onPress: () => void,
  rightContent?: string,
  shortItem?: boolean,
  subtitle?: string,
}
