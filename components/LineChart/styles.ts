import { ViewStyle } from 'react-native';

export const chartHeight = 200;
export const xAxisHeight = 30;

export const chartWrap: ViewStyle = {
  height: chartHeight,
  flexDirection: 'row',
};

export const lineChartWrap: ViewStyle = {
  marginBottom: xAxisHeight,
};
